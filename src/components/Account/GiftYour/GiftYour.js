import classNames from 'classnames/bind';
import styles from './GiftYour.module.scss';

const cx = classNames.bind(styles);

function GiftYour() {
    return <div className={cx('wrapper')}>GiftYour</div>;
}

export default GiftYour;
